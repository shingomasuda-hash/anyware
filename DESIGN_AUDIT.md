# DESIGN AUDIT — v1実装がテンプレートに見える理由

v1（THE UPDATE DISTRICT）の実装を自己監査した結果。デザイン全面刷新（v2 THE ANYWARE
HOUSE）の起点として記録する。

## 現状の問題点

- **ファーストビューが「文字＋小さな挿絵」で構成されている**：`HeroDistrict.tsx` は
  画面の建築イラスト（`DistrictIllustration.tsx`）が下部62%のみに小さく収まり、実質は
  「巨大な見出し＋本文＋ボタン2つ」という一般的な企業サイトのヒーロー構成に近い。
  参考画像のような「画面の80%以上を建築空間が占める」状態になっていない。
- **建築表現が単一の平面SVGバンド**：5部屋が横一列の「窓のついた壁」程度の情報量しかなく、
  断面構造・奥行き・手前中景奥のレイヤー分離がない。
- **BUSINESSセクションがカードカルーセル**：指示された「建物内をスクロールで巡る」体験ではなく、
  横スクロールする角丸カード（`BusinessZone.tsx`）＝典型的なカードUI。
- **WORKSセクションも均一カード**：`WorksArchive.tsx` は幅の異なるだけの同一コンポーネントの
  横スクロールで、雑誌的なレイアウト差がない。
- **HOW WE WORKが番号付きの横並び**：`EcosystemFlow.tsx` はグラデーション線＋丸番号という、
  一般的な「プロセス4〜5ステップ」テンプレートそのもの。
- **MVVセクションが単純に大文字を縦に並べただけ**：建物比喩はSVGを薄く背景に敷いているのみで、
  「各部屋に順番に光が灯る」体験になっていない。
- **CONTACTセクションが単色背景＋中央揃えテキスト＋丸ボタン**：白背景ではないが、
  「クレイ色の背景に中央揃えの文字とボタン」という構造自体はテンプレートと変わらない。
- **カラーが平坦**：セクションごとに単色背景を切り替えているだけで、「照明の色温度が
  空間ごとに変わる」という質感がない。
- **モーションがほぼ全てopacity/translateYのフェードイン**：`useInView`ベースの
  フェード＋わずかな移動のみで、マスク、光の点灯、視点移動などの意味のあるモーションがない。
- **全体的にTailwindのユーティリティを組み合わせただけの見た目**：角丸、細いボーダー、
  均等パディングなど、Tailwindサンプル的なパターンが随所に残っている。

## v2での方針

上記を踏まえ、以下を全面的に作り直す：

1. `HeroDistrict` / `DistrictIllustration` を全面書き直し、8レイヤー構成の建築断面シーンにする。
2. `BusinessDistrict` をGSAP ScrollTriggerによるピン留め＋部屋フォーカス切り替えに再構築する。
3. `WorksArchive` を index に応じてレイアウトパターンを変える編集的グリッドに再構築する。
4. `EcosystemFlow` を光/水路のパスに再構築する。
5. `MVVSection` を建物俯瞰＋部屋ごとの点灯シーケンスに再構築する。
6. `ContactGateway` をエントランス/受付の空間表現に再構築する。
7. `SiteHeader` / `MobileMenu` を建物案内図メニューに再構築する。
8. カラートークンを拡張し、部屋ごとに異なる「照明温度」を持たせる。

文章・データ構造・ルーティング・SEO・フォーム機能は変更しない。

## v3（ANYWARE DISTRICT）：v2がまだシンプルすぎると判断された理由と対応

ユーザーから提供された参考動画（「MAISON」というフォトリアルな3DCG建築ショーケースサイト。
円形の建物を回転させながら図書室・寝室・キッチンを巡る）を分析した結果、以下の差分を
特定した。

- 参考は**写真品質のフォトリアル3DCGレンダリング**（実在の3Dアセット・HDRI照明を用いた
  Three.js/Spline等の作品）。本実装はSVG/CSSによる2.5Dベクターイラストであり、
  画像生成ツールや3Dアセットライブラリを持たない本環境では、ピクセル単位で同等の
  写実性には到達できない。この制約は正直に記録しておく。
- 一方で「情報密度」「奥行き（前景・中景・背景）」「明暗差」「巨大文字の物体感」
  「床への反射」「円みのある建築」という**デザイン文法**は、ベクターイラストの範囲でも
  大幅に強化可能と判断し、以下を実施した。

### 実施した変更

1. **建築外形を樽型（円みのある側壁＋大きく高いアーチ屋根）に変更**
   （`DistrictIllustration.tsx` の `shellPath`）。直線の側壁＋緩いアーチから、
   ベジェ曲線で膨らむ側壁＋高いドームへ。
2. **各部屋の情報密度を2〜3倍に増加**：棚、地図、人物、掲示物、本棚、器、機材などを
   RoomProps に追加。
3. **床の反射（鏡面）を追加**：`mask`+`scale(1,-1)` で什器を反転・フェードさせ、
   高級建築レンダリングに特徴的な「床に映り込む」表現を実装（Hero/BusinessZoneScene共通）。
4. **光だまり（light pool）を追加**：天井照明の下に radialGradient
   の柔らかい光溜まりを配置し、「照明が空間を照らしている」感覚を強化。
5. **巨大ANYWARE文字の視認性を改善**：背後にamber系のglowを追加し、建物の足元に
   直接立つ位置まで持ち上げ、初期ビューポート内で全体が視認できるよう再配置。
6. **遠景の街並みを画面全幅に再配置**（以前は`preserveAspectRatio="slice"`による
   クロップでほぼ画面外に消えていた実装バグを修正）。
7. **重要なバグ修正**：`DistrictIllustration`と`BusinessZoneScene`は同一ページに
   複数インスタンスが同時マウントされる（例: ホームのHeroとMVVセクション、
   WORKSセクションで同じ事業カテゴリーの実績が複数枚並ぶ場合）。グラデーション/マスク/
   クリップパスのIDがインスタンス間で衝突する不具合があったため、`useId()`で
   全IDをインスタンス単位にスコープした。

### 今後、真のフォトリアル3DCGに近づけるには

- 実際の3Dモデル・HDRI環境光を用いたThree.js/React Three Fiberシーンの構築
  （現状は意図的に見送り。低品質なローポリゴンになるリスクが、密度の高い2.5D
  イラストより悪化すると判断したため）。
- または、`IMAGE_GENERATION_PROMPTS.md` の内容を元に生成・撮影した高品質な
  写真/レンダリング画像を、建築フレーム内にレイヤーとして差し込む。

## v4：ファーストビューを支給の本番メインビジュアルへ差し替え

クライアントから実写品質のメインビジュアル（PC用/SP用、`public/images/hero/`）が
支給されたため、上記v3で課題としていた「フォトリアルな質感の壁」がファーストビューに
限り解消した。`HeroDistrict.tsx` を全面書き直しし、SVGベースの建築イラスト
（`DistrictIllustration`/`DistantSkyline`/`ForegroundProps`）は削除して、支給画像を
背景とする9レイヤー構成（背景画像／暗部グラデーションマスク／ガラス反射／光の揺らぎ／
グレイン／浮遊粒子／コピー／CTA／スクロールガイド）に置き換えた。

- GSAP + ScrollTrigger を新規導入（初期表示のスケールイン・マスクからのコピー出現・
  窓明かりの点灯風フレア、マウス追従の視差、スクロール連動のズーム＋コピー退場）。
- PC/SP は同じ画像を縮小するのではなく、支給された別カットの画像をそれぞれ
  `md:` ブレークポイントで出し分け（アートディレクション切り替え）。
- CTAは角丸ボタンではなく `.sign-plate`（案内板／金属プレート調）スタイルを継続使用。
- ファーストビュー以外のセクション（Philosophy以降）は今回のスコープ外のため変更なし。
