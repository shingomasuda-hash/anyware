# CONTENT MAPPING — 旧サイトからの移行対応表

## 重要な制約（必ず先にお読みください）

本実装を行ったセッションの実行環境では、ネットワークポリシーにより
**`https://any-ware.jp` への直接アクセス（WebFetch / curl 等）がすべて403で拒否**されました。
そのため、旧サイトの実ページを直接クロールしてページ構成・本文・画像・フォーム送信先・
計測タグ・OGP設定・構造化データ等を確認することができませんでした。

確認できた情報は、検索エンジン（Web検索）が索引していたページタイトルと要約のみです。
**確認できない実績・数値・取引先名・URL構成を創作することはしていません。** 公開前に、
実際に旧サイトへアクセスできる環境で以下の対応表を必ず検証・更新してください。

## 検索経由で確認できた事実

| 項目 | 内容 | 出典 |
|---|---|---|
| サイトの性質 | 大阪・京都エリアの飲食店・中小企業向けブランディング/SNS運用/広告運用等の支援会社 | 検索結果のタイトル・要約 |
| 会社概要ページ | `https://any-ware.jp/company/` が存在 | 検索結果URL |
| お知らせ一覧 | `https://any-ware.jp/news/` が存在（WordPress、日付形式パーマリンク `/YYYY/MM/DD/slug/` を使用） | 検索結果URL |
| 事例記事1 | 「古民家カフェ『静カフェ』：SNS運用で売上600%アップ」`/2025/04/03/` | 検索結果タイトル |
| 事例記事2 | 「外資系ホテルのSNS戦略：センタラグランドホテル大阪」`/2025/04/11/` | 検索結果タイトル |
| 事例記事3 | 「BtoB製造業のSNS挑戦：TikTokブランディング」`/2025/05/12/` | 検索結果タイトル（要約に社名の記載があったが原文タイトルでは確認できず、本サイトには非掲載） |
| 事例記事4 | 「地方で事前のPRも行わず、初月から黒字化を達成」`/18/` | 検索結果URL |

これらは `src/data/works.ts` / `src/data/news.ts` に、確認できた文言のみを転記して掲載しています。

## 確認できなかった項目（要対応）

以下は本サイトでは「確認中」「準備中」として意図的に空欄・null にしています。判明次第、
該当データファイルを直接更新してください（サイト内にハードコードされている箇所はありません）。

| 項目 | 該当ファイル | 現在の状態 |
|---|---|---|
| 所在地・電話番号・代表者・設立年・資本金 | `src/data/company.ts` (`companyFacts`) | `null`（会社概要ページで「確認中」表示） |
| 問い合わせメールの送信先 | `.env` (`CONTACT_TO_EMAIL`) | 未設定。設定するまでフォームは正直なエラーを返す |
| 公開連絡先メールアドレス | `.env` (`NEXT_PUBLIC_CONTACT_EMAIL`) | 未設定 |
| SNSアカウント（X, Instagram, Facebook, note等） | `src/data/site.ts` (`social`) | `null` |
| Googleアナリティクス等の計測タグ | 未実装 | 計測IDが判明次第、`src/app/layout.tsx` に追加してください |
| 既存のプライバシーポリシー本文 | `src/app/privacy/page.tsx` | 一般的な雛形に差し替え済み。法務確認が必要 |
| 採用情報の具体的な募集職種・応募方法 | `src/app/recruit/page.tsx` | 一般的な採用訴求のみ掲載。具体的な求人票は別途追加が必要 |
| 旧サイトの正確なURL一覧・サイトマップ | 不明 | 下記「URL移行方針」を参照 |

## URL移行方針

新サイトのURL構成（英語slug）は指示書のサイトマップ（11章）に準拠しています。

| 新URL | 想定される旧URL |
|---|---|
| `/` | `/` |
| `/company` | `/company/` |
| `/news` | `/news/` |
| `/news/[slug]` | `/YYYY/MM/DD/[wordpress-slug]/`（WordPress日付パーマリンク） |
| `/works`, `/works/[slug]` | 数値ID形式のページ（例: `/18/`）だった可能性あり |
| `/business/*` | 不明（旧サイトのサービス紹介ページ構成が確認できていない） |
| `/recruit`, `/contact` | 不明 |

`next.config.mjs` の `redirects()` は、旧URLが未確認のため**意図的に空**にしてあります。
旧サイトへアクセスできる環境で全URLを洗い出し次第、以下の手順で301リダイレクトを追加してください。

1. 旧サイトのXMLサイトマップ（`https://any-ware.jp/sitemap.xml` 等）または
   Google Search Consoleのインデックス済みURL一覧を取得する。
2. 各URLと新URLの対応を本ファイルの表に追記する。
3. `next.config.mjs` の `redirects()` に `{ source: "/旧パス", destination: "/新パス",
   permanent: true }` を追加する。
4. 特に検索流入のある「ブランディング関連」「飲食店コンサルティング関連」「支援実績」
   「会社概要」「採用」「お問い合わせ」「新着情報」の各ページは優先的に対応する。

## 再利用した資産

- 実績記事のタイトル・成果表現（数値含む）：`src/data/works.ts` に転記（出典URL付き）。
- ロゴ・既存写真：本セッションでは取得できなかったため未使用。`IMAGE_GENERATION_PROMPTS.md`
  に差し替え箇所を記載。
